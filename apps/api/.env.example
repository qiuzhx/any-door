# Copy to `.env` locally (never commit real secrets)
OPENAI_API_KEY=
# Optional: Azure / OpenRouter / 国内中转等兼容 OpenAI 协议的网关
OPENAI_BASE_URL=
# Optional — default gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
# Optional — default shown when客户端未传 system 消息
# CHAT_SYSTEM_PROMPT=
